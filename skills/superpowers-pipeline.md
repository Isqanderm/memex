---
name: /arch:superpowers-pipeline (DEPRECATED)
description: "DEPRECATED — renamed to /arch:start. Load /arch:start instead."
version: 1.0.1
author: Hermes Agent
license: MIT
---

# ⚠️ DEPRECATED — RENAMED TO `/arch:start`

**This skill has been renamed.** Load `/arch:start` instead.

The content now lives at [`skills/start.md`](skills/start.md).

Everything else — the pipeline flow, the three phases, the handoff directives — is identical. Only the name changed: `/arch:superpowers-pipeline` → `/arch:start`.
